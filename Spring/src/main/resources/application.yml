server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/easy_order?characterEncoding=utf8
    username: machine
    password: learning
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true
        use_sql_commnets: true

  output:
    ansi:
      enabled: always
